import { Columns } from "./table";
import { IView } from "./view";
export declare function dateToSQLUTCFormat(date: Date): string;
export declare const composeCreateTableStatements: <Tables extends {
    [name: string]: Columns;
}>(tables: Tables) => string[];
export declare const sortTableDependencies: (tables: Record<string, Columns>) => [string, Columns][];
export declare const sortViewDependencies: (views: IView<any>[]) => IView<any>[];
